server:
  port: ${WalletUserApplication:8080}
  netty:
    connection-timeout: 2m
spring:
  main:
    banner-mode: off
  config:
    activate:
      on-profile: 'test'
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/wallet-user}
      auto-index-creation: true
